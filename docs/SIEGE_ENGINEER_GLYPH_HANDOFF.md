# Siege engineer glyph

Branch `codex/siege-engineer-glyph`, base `ad578e6`.

Engineers show crossed hammer/spade tools instead of a wheeled cannon-like shape,
matching the current catalog's works-building and fortification-breaching role.
Four existing glyph slots are reused; artillery icons remain unchanged.

Warfare runtime probe PASS, including actual hammer/handle/spade mesh assignment
and existing repeated-update, silhouette-offset and marker-budget checks. Hidden
self-quitting all-eleven-unit capture inspected: `artifacts/military-engineer-tools.png`.
Previous comparison: `artifacts/military-all-unit-silhouettes.png`.

No rules, unit classification, counts, saves, menu or selection changes. Existing
shutdown resource warnings remain. Shared local_terrain.gd edit is limited to the
siege_engineer glyph branch. Physical unit figures are not changed.
