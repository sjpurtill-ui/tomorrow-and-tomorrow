# Fleet and air-command preparation preview

READY from `codex/service-preparation-preview`, base `3a7257f332104f4f1a2f89aab2926062ac3183cb`, worktree `/Users/seanpurtill/Documents/Codex/tt-command-order-state`. The source commit is recorded by the canonical integration entry after merge.

Owned files: `scripts/hud/command_preparation.gd`, `scripts/hud/command_hierarchy_panel.gd`, the read-only readiness query in `scripts/joint_operations.gd`, `tests/test_command_hierarchy.gd`, and this handoff. No shared integration hotspot edits or conflicts.

Selecting a Fleet or Air Force command and a next objective now shows compact training, condition and range-coverage meters for its next zone, plus the full selected-command fuel requirement and shared reserve. The first preparation warning is visible, with named subordinate reasons in the tooltip. A headquarters cannot count the same six reserve fuel as sufficient for each of two four-fuel wings: the preview identifies the total eight-fuel requirement and two-fuel shortage. Values update as staff progress, equipment changes or fuel arrives, without changing drafts or current orders.

Virtual subdivisions use their exact previewed craft and report assembly restrictions without splitting the parent. Clearing the next zone does not borrow the old order's region. Suspended initial instruction, repairs, travel, transport commitments and range failures use the existing authorities. Valid objectives may still be issued while preparation is incomplete; staff retain control of training and execution. Unsupported missions remain disabled by the existing capability check and do not display a green preparation message. Land command stays unchanged; stand-by selections omit the preview.

The reusable readiness query now ignores zero-count craft when estimating initial training time and matches the existing staff repair threshold (at least 80% condition). This changes the warning to agree with daily execution; it does not change repair/training policy or duration. No orders, fuel reservations, extra troops, replacement equipment or save fields are created by the preview.

Worktree validation with isolated userdata `TomorrowServicePreparationTests`: **132 tests pass**, zero errors/failures/skips/orphans, exit 0, across command hierarchy, joint operations, joint campaign loop, training strategy and main-map services (`/tmp/tt-service-preparation-tests.log`). Six new regressions cover shared fuel budgeting/read-only state, exact detachment craft and assembly, empty next-zone/suspended training, live meters/resupply/draft preservation, staff repair-threshold agreement/zero-count training estimates, and service isolation. Existing 1024×640 and 1280×720 layout checks include the visible meters and confirm the primary order action remains reachable. `git diff --check` passes; the owned test override is removed before commit.

The preview is advisory, not a guarantee of mission success. Terrain routing and mission assignment still perform final validation. Training is weighted by crew; condition and range coverage by craft. Coverage is not air/naval control. The fuel budget is the selected force's full daily mission requirement; other forces and exercises also spend the reserve, and missions may wait or run at reduced efficiency. No numerical HOI4 parity or native visual inspection is claimed. No player/editor was launched, stopped or replaced.
