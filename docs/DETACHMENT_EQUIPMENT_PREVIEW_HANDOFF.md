# Exact naval and air detachment equipment

READY from `codex/detachment-equipment-preview` in `/Users/seanpurtill/Documents/Codex/tt-command-order-state`, based on `699eefa93ecf047d4f71adcef4f12ab2b799af84`. Owned: service allocation/preview/assignment in `scripts/command_hierarchy.gd`, selected-equipment and capability presentation in `scripts/hud/command_hierarchy_panel.gd`, focused hierarchy tests and this handoff.

Virtual naval/air subdivisions now preview the craft they would actually receive. Preview and real detachment share the same pure allocation function, preserving the existing inventory ordering, original-force remainder and replacement targets. The selected-command tooltip lists the positive craft counts and updates if composition changes without a headcount change. Mission availability uses the selected subdivision's craft instead of borrowing its parent's capabilities.

Assignment checks exact detached composition before reorganizing anything, including mission capability and operating range. A balloon-only element cannot receive a fighter mission; a fighter element can reach a region within fighter range even if its unsplit parent also contains short-range balloons. Validation still restores the original force after trial checks, and unsupported/out-of-range orders leave assets and hierarchy unchanged. No direct land controls or new logistics forms are introduced.

Worktree validation: **68 tests pass**, zero errors/failures/skips/orphans, exit 0, across `test_command_hierarchy`, `test_main_map_services`, `test_map_panel_dismissal`, `test_joint_operations` with explicit worktree path and isolated userdata. Log `/tmp/tt-detachment-equipment-tests.log`. Five new tests cover exact mixed-craft previews, unsupported order refusal before splitting, nested naval and air materialization matching previews, replacement/crew conservation through JSON save/load, detached-range validation and read-only UI refresh. Existing native tree expansion, layout, land campaign, cancellation and service tests pass. `git diff --check` passes; owned override removed.

No save schema changes: equipment is added only to the read-only preview, not stored in hierarchy records. Existing saves and original split ordering are retained. No shared hotspot conflicts at handoff. This resolves the parent-capability limitation recorded in COMMAND_MISSION_CAPABILITIES_HANDOFF.md. It is not a complete readiness forecast: base readiness, transport commitments, assembly availability, training, fuel and repairs continue to use their existing authorities. No numerical HOI4 parity or native player UI inspection is claimed. No player/editor is interrupted; canonical integration and release verification are recorded separately.
