# 2026.09.10.5 — smoother woodland detail

Fixed a square patch of dark tree speckles visible at 10,000 ft on wide displays. The normal aerial distances now use the existing biome ground cover consistently. When zooming closer manually, crowns, scrub and forest floor fade in together, with a soft boundary around the detailed area.

Zooming preserves the plants already constructed. Hidden aerial views and unchanged settlement upkeep avoid rebuilding close vegetation; actual clearing is applied when that detail becomes visible again. Seasons, fog, resources and physical geography retain their existing rules.

The change passes 41 tests in both the task checkout and canonical main, an isolated normal-entry headless boot, and native canopy, seasonal and four-distance terrain checks. The native checks cover real temperate, tropical and dryland sites, both standard and wide 10,000 ft views, stable paused images and plant identities. This is a specific rendering correction; mature-campaign frame stalls and broader landscape improvements remain ongoing.

No save migration is required. The approved ancient Scouting visual direction remains a separate interactive mockup and is not part of this release. The normal standalone package is built from the integrated revision recorded in its `build.ok`; no player session is restarted automatically.
