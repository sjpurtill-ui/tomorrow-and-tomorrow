# Standalone macOS player launch

READY for integration. Worktree `/Users/seanpurtill/Documents/Codex/tt-command-order-state`, branch `codex/macos-release-launcher`, base `d9e0f44ab88014ab4ad293a060b340010f716632`. The user explicitly requested a fresh normal game without the editor or debugger.

Adds the macOS Release export preset and `tools/launch_game_macos.py`. Normal launches require canonical Mac main, clean tracked changes, the normal main scene, no test override and no other player. The launcher exports a true release app into the canonical checkout's per-commit artifacts, verifies its ad-hoc signature, records successful builds, and opens a fresh world by default. `--resume-saved` is explicit; `--build-only` permits isolated development exports without opening a player. Official release templates reject `--path`; the absolute executable path identifies the canonical source/build, and its bundled project supplies the main scene. No editor/debug/embedding arguments are passed.

The official Godot 4.7.2 macOS template is installed from the [official release](https://godotengine.org/download/archive/4.7.2-stable/). Download SHA-256 verified against the official GitHub asset metadata: `f298490b8d44d934be425a5a65a51bf15f422428b229a06a6e11d9ffea248011`. The release binary's own help identifies it as a release export template. No local Godot editor executable is substituted for a release build.

`project.godot` retains the main scene and Compatibility renderer, enables the extra ARM-required ETC2/ASTC texture import format, and updates the visible release version to 2026.09.08.1. This is the shared-file conflict to review. No simulation or save-schema changes. Owned files: the preset, launcher, `project.godot`, `docs/MAC_SETUP.md` and this handoff.

Validation: worktree release export succeeds, `codesign --verify --deep --strict` passes, Python compilation passes, and an attempted worktree player launch is correctly refused. The packaged main game was checked headlessly and exited; `/tmp/tt-macos-release-final-smoke-stdout.log` confirms version 2026.09.08.1, fresh day 0 and the real opening scene. No script/parser/startup failures. The forced three-frame exit reports two ObjectDB instances and one resource still in use; this short-shutdown cleanup warning is not claimed fixed. No graphical test scene was opened.

At the explicit restart request, the old editor's normal Quit flow displayed “Stop running project before exiting the editor?” and Stop & Quit was confirmed. Editor 2812 and embedded debug player 6076 exited; no campaign save was overwritten. Canonical export, live release launch and native visual verification follow integration. This locally signed app is not a notarized public distribution. Future gameplay integrations require rebuilding/relaunching the release app; a running release does not load source edits.
