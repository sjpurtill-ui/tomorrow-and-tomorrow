# Compact illustrated military roster

Worktree `/Users/seanpurtill/Documents/Codex/tt-military-visual-roster`, branch `codex/military-visual-roster`, base `6d127d8f9b3e6c00d25a8cfe2fa458f3d8213735`. Sole integrator; no concurrent worker edits.

The roster now uses approximately 96px force rows, painted portraits, pictorial strength/equipment/drill meters, concise duty and shortage status, and inline details on selection. A small force list shrinks the panel to leave the map visible. Stable row IDs keep live updates from rebuilding the entire list; filters remain current. Dated field reports and unknown formations retain their existing information authority. Separate Army/Navy/Air views have distinct scenery, insignia and colors. Training strategy uses visual commitment cards while the existing staff-run simulation retains control of duration, costs and attendance. Map commands, management navigation, Escape and outside-map dismissal remain available.

The 15 new representative paintings and three service backgrounds are integrated assets. Prompts and built-in imagegen provenance are in `assets/ui/military/ARTWORK.md` and `assets/ui/military/portraits/PROMPTS.md`. Portraits import at a bounded 512px size with mipmaps. Types without matching artwork use service scenery and role insignia. No 3D world meshes are rendered in this UI. This is not 50 individually painted unit classes, a replacement of world models, or a simulation change.

Validation: 41 cases pass across military visual roster, training strategy, map panel dismissal and main-map services; zero errors/failures/skips/orphans (`/tmp/tt-painted-roster-final-tests.log`). Tests cover 800–1600px layouts, compact row/panel dimensions, current filters, live selection, actual counts, missing reports, art availability, policy isolation and qualified crew exercises. Native capture-only probe exercises actual roster/policy widgets, small-window resizing and the painted thumbnails; captures live under ignored `artifacts/military-roster`. Test uses isolated userdata and Dummy audio, closes itself, and is never the player game.

Save compatibility: no schema or state migration; presentation only. Shared-file conflicts: none; `project.godot` version bump belongs to final integration. Runtime tests neither spend resources nor advance the user's game. Integration and release build verification must precede a player-build claim.
