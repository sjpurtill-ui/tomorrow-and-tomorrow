# Release 2026.09.05.5

Canonical player project: C:/Users/sjpur/TomorrowandTomorrow, main.
Integration base: c89b895. Candidate: codex/integrate-latest-20260905 in
C:/Users/sjpur/tt-integrate-latest. Source delivery: 7aef120 through 018cedd
on codex/city-defense-aftermath, with the test-only project.godot excluded.

## Delivered

Native battle and siege interfaces, formation orders, round presentation,
state-preserving replay, loss explanations, paginated controls, and linked
historical siege/battle review. Existing terrain, units, animations, generals,
civics, cities, diplomacy, scouting and earlier release features are retained.
Army inspection remains separate from battle resolution.

Saved military notifications now preserve fractional campaign time on reopening.
Previously, restoring pending aftermath rounded day 9432.03296121855 back to
9432.0. Live fast-forward interruption retains its existing batch-stop behavior.

The launcher requires canonical main and the real world scene, rejects local
test settings overrides, and refuses to open a second canonical player over an
existing session. Use the desktop Play Tomorrow and Tomorrow shortcut.

## Audit and unfinished work

All registered worktrees were checked for patch-equivalent commits and tracked
local changes. Most differing branch hashes are already integrated deliveries.
The historical non-equivalent civic/scout/chart/city-intelligence commits are
accounted for by FEATURE_RECONCILIATION.md and prior release records, including
deliberate conflict resolutions. Do not merge their older whole systems again.
The dirty military-usability code is an older version of live bindings and
training availability already present on main. Preserve it as reference.

The only new completed gameplay delivery found was the battle/siege HUD series.
The worker project's alternate main scene and custom save directory were not
integrated. Manual test fixtures are guarded against the normal user directory;
they remain test-only and cannot be started by the player launcher.

HELD: outpost and sliced-vegetation prototypes, unfinished Blender source export,
unrelated economy work. PENDING, not implemented: continuous actual-world city
encounters and clearer faction uniforms. Siege rendering still uses its existing
separate city representation. This release does not claim those requests shipped.
No old worktree or uncommitted work was deleted or reset.

## Validation and save protection

Candidate regression: 174 cases across 12 suites, zero errors, failures, skips
or orphans. Real GPU mouse battle and siege flows passed on private desktops,
including target/orders, single-round resolution, replay state equality,
aftermath fixture, repeated Maintain, real time controls, export/import/reopen,
compact layouts, assault, outcome and linked historical review. Rendered battle
and siege captures were inspected. All probe processes exited.

The real saved campaign was copied into a separate test user directory. The
first resume check exposed the fractional-time issue above; the corrected GPU
check passes. It preserves exact time, population405, seed1792400273, SeanTown,
research choices, known discoveries and city management settings.

Original quicksave SHA256:
4FF73952D6B50FD130B4AEC97BDE5B5EDC68A0058DC59ACF39F3A39DD1B047D5.
Backup: canonical artifacts/pre-release-2026.09.05.5.save.
Evidence: artifacts/release5-* logs, screenshots and worktree audit. Earlier
repository-wide baseline failures are recorded in release .4; this release
does not claim a clean run of every repository test.

## Worker handoff

Copy docs/WORKER_PROMPT.txt. One integrator owns main. Workers start from current
main in separate worktrees with bounded ownership and return tested commits.
Only a verified integration is called delivered. No branch preview is the game.
