# Release 2026.09.11.3

Terrain rendering skips surface calculations under completely opaque fog and fine soil/clearing calculations once their existing detail weight reaches zero. This preserves the landscape's appearance while reducing wasted rendering work.

In a guarded, paired native material comparison at 1280×720, distant known terrain's frame interval was 30–34% lower; fully hidden terrain was 86–91% lower. These are isolated render-loop results, not populated-campaign FPS. Daily simulation stalls remain outside this change. The GPU timer is unavailable on this Compatibility run.

Hidden pixels remain byte-identical. Visible captures are identical or differ only by one 8-bit channel value in at most 9 of 921,600 pixels. Real woodland, dryland, coast/fog-edge and full landscape captures were inspected. All four distance, coverage, cancellation, fog and physical-world-boundary checks pass. All 41 worktree and 41 canonical regression cases pass with no errors/failures/flaky cases/skips/orphans; ordinary imports and normal-entry boots are clean.

Integrated source: `f3345c92c25d85ba2c1f45d4b3d229b56f66abfc`. See `TERRAIN_SHADER_COST_HANDOFF.md` for exact measurements, reproduction and limits. No assets, physical geography, resources, simulation rules, cache budgets or save schema change. Prior woodland crown detail is retained. Landscape realism and broader performance work continue.

Both owned overrides are removed before packaging through the canonical Mac launcher's build-only mode. Its `build.ok` records the exact source revision. No player/editor is launched, stopped or restarted. Art tasks remain stopped and unintegrated.
