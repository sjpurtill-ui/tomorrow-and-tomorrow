# Equipment-aware command objectives

READY from `codex/command-mission-capabilities`, worktree `/Users/seanpurtill/Documents/Codex/tt-command-order-state`, based on `bf56e14eabf62b45f529a23f6871d735583015ac`. Ownership: `scripts/hud/command_hierarchy_panel.gd`, its focused regression coverage in `tests/test_command_hierarchy.gd`, and this handoff.

Fleet and Air Force objective menus disable missions unsupported by the selected command's ships/aircraft, using the existing `joint_operations.missions_for` authority. Headquarters require compatibility across their real subordinate commands. Menu tooltips name up to three incompatible commands and count the remainder. A retained draft that becomes incompatible shows a short red reason and cannot be submitted; selecting a compatible subordinate or replacing lost equipment restores availability. Refresh, selection and preview never issue orders or replace the draft. The assignment handler rechecks so right-clicking a map zone cannot bypass the feedback. Empty/stale selections are disabled; land objectives retain their existing mission catalog and execution.

Validation: **63 tests pass**, zero errors/failures/skips/orphans, exit 0, with explicit worktree path and isolated userdata. Suites: `test_command_hierarchy`, `test_main_map_services`, `test_map_panel_dismissal`, `test_joint_operations`. Log `/tmp/tt-mission-capabilities-tests.log`. Five new cases cover balloon/fighter roles, mixed headquarters and virtual selection, read-only refresh after loss/replacement, real naval assignment and empty/land selection. Existing compact-layout test now includes the longest visible capability message at 1280×720 and 1024×640. Current-order editing, native tree expansion, exact selection, all existing campaign/crew/save/movement tests in these suites pass. `git diff --check` passes; owned override removed.

This is capability feedback, not a complete deployment/readiness forecast or HOI4 numerical parity. Current mission validation still checks routes, bases, operating range, transport commitments and actual detached composition. Virtual subdivisions use their unsplit parent force's capability until materialized; the authoritative assignment can still refuse a specialized mission if that exact detachment lacks the required craft. Training, fuel and repairs can delay execution after assignment. No simulation/save format changes or shared-hotspot conflicts. Native inspection of the changed player UI is pending the next launch; the current player is left untouched. Integration and source hashes are recorded separately.
