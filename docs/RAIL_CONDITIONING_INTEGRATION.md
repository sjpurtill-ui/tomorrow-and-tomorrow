# Rail freight and conditioned food integration

Canonical Mac checkout `/Users/seanpurtill/Documents/Codex/tomorrow-and-tomorrow`, main runtime `e94ffa8e5d65684bf62e7f73b58a0c7b5ec77fc3`. Integrator worktree `/Users/seanpurtill/.codex/worktrees/ef8f/tomorrow-and-tomorrow`, branch `codex/grain-conditioning`, base `50bbb263a38157fae35817a22795f5a55dc80495`.

The complete frozen rail range after `368b771` (foundation `6eb949b`, completion `1c161efa75db188b7251574a8f6861eb72cfccbf`) is combined with food conditioning `0635688` in merge `fbaf803`. One additive CivilianIndustry conflict was resolved by retaining all eight rail recipes plus every record-media and geared-workshop recipe. Discovery registration retains all three families; TechnologyOperations service bounds and replacement-input validation remain unchanged.

Five rail identities now pay for surveyed timber track, ballast, gauge-specific wheelsets/wagons and brakes. Actual owned-city shipments use finite loaded and return-trip capacity; cargo and crews cannot be duplicated between road and rail modes. Monthly construction and maintenance share existing builders. Occupation holds cargo and fleet commitments, while new dispatch resamples the supported alignment. The installation controls, automatic investment, paid manufacture and whole-game human/actor saves are tested. Human haulage drops the earlier animal-harness prerequisite only; its old predicate is preserved in promotion reconciliation. See `RAIL_FREIGHT_RUNTIME_PLAN.md` for the 120-km, 2%-grade bounds and missing bridges, tunnels, network signaling, engine and military services.

Two food identities now consume actual grain/plants, water, heat and shared handling to produce finite conditioned lots. Wet lots remain unavailable through their delay and need later supplied drying. Weather limits a paid solar chamber, whose daily quota is shared by loading and finishing all lots. Fuelled grain drying is an alternative. Saved wet lots survive whole-game binary saves; the existing strict validator is retained. See `GRAIN_CONDITIONING_HANDOFF.md` for aggregate crop representation and game-balance limits.

Validation: `/tmp/tt-rail-conditioning-results.json` contains 94 passing combined runtime cases: rail 16, food batches 30, city resources 13, operations 16, owned simulation 19. Earlier 20 grain and 14 food-preparation cases bring distinct runtime checks to 128. Canonical `/tmp/tt-rail-conditioning-canonical-results.json` contains 71 passing cases: rail 16, food batches 30, city resources 13 and atlas 12. Zero errors, failures, skips or orphans. Graph `/tmp/tt-693-graph.log` has no dependency errors or blocked production; `/tmp/tt-693-boot.log` reaches the ordinary direction screen in headless mode. Both art imports finish cleanly. All 693 runtime definitions exactly match `/tmp/tt-693-canonical-snapshot.json`; all 15 ledger-tool cases pass.

Five artwork assets from frozen `d33c5e0fda9287469fd06bd76d9e1a636616c0d4` were visually reviewed and integrated as `e94ffa8`. They depict aggregate road preparation, gauge checking, timber track laying, human wagon haulage and manual brakes in the approved sparse paper/gouache series. Each imports at 768 pixels with mipmaps. Art inventory is 115 verified /693 live, with 578 queued.

Ledger: 693 integrated + 2,446 drafts = 3,139 distinct identities; 1,861 remain to author and 4,307 to implement toward 5,000. Existing saves accept absent optional rail/food-batch state; installed paid fabric, cargo and wet food remain owned through continuation. No player/editor was launched or interrupted, no package rebuilt and no long historical-pacing result is claimed. The separate previously documented river-edge visual fixture remains outside this scope.
