# Landscape iteration 2 — sea level and coastal water

Task worktree: `/Users/seanpurtill/Documents/Codex/tt-coastal-water`, branch `codex/coastal-water`, base `4edac93deb789201384a89b9a91921d7dda8d343`. Sole integrator. Owns the water construction/binding in `scripts/local_terrain.gd`, new `scripts/coastal_water.gdshader`, focused tests and native capture probe. No other task edited the shared terrain file.

The seamless ocean formerly rendered at sea level plus 0.012 km: twelve metres above the physical datum. It now renders at the actual sea level. A native regression shows 6.6 m coastal ground dry at the corrected surface and submerged when the old height is reinstated. Physical terrain, founding clearances, freshwater rules, navigation, resource ledgers and civilization mechanics are unchanged.

Water color now follows the uploaded visible regional terrain heights. Muted shallow water, shelf water and deep water blend by depth; both alternating triangle diagonals agree with the rendered mesh. The color fades to deep water beyond the regional patch's outer margin, without inventing a shallow band at a texture edge. At continental pixel footprints, bathymetric coloring resolves to ocean color before the moving regional patch could appear as a square. Patch installs and cached revisits rebind the same height authority. A bounded local water mesh (at most 8,192 triangles) replaces the far ocean inside the regional patch, retaining bounded projection geometry and matching cutouts. Both materials use the existing fog registry.

Subtle warped wind patterns and ripples are world anchored. Derivative filtering removes unresolved animation at distant map scales. The wave shader changes shading only, never the physical water height. Unknown water hides depth colors and specular/moving details.

Validation: **35 tests pass**, zero errors/failures/skips/orphans, in coastal water, rendered surface sampling, terrain patches, river geometry, close terrain, landscape cover and founding water guidance (`/tmp/tt-coastal-final-tests.log`). The normal project boots and exits headlessly without errors (`/tmp/tt-coastal-main-boot.log`). Native Compatibility rendering on M1 Pro passes: 48 land/water shoreline samples, reproduction of the old flooding, three depth colors, four GPU-versus-mesh-ray interpolation comparisons, full water coverage at all four distances, no unresolved animation at Region/Continent, and identical unknown-water pixels when hidden depths change. Top-down and perspective coastal captures plus four water-scale captures were inspected. Probe exits and captures remain ignored under `artifacts/coastal-water`.

The controlled coast is a rendering fixture, not a claim to have audited every generated world. Its orthographic camera uses a bounded far plane; a planet-wide orthographic far plane would itself quantize the depth buffer by metres. The perspective capture uses the game's 35-degree FOV and 100,000 km far plane. These views are capture-only tests, never the player game.

Save schema and world state are unchanged. Existing worlds receive the presentation correction after a normal restart. This is not a new ocean/tide/weather simulation, a navigability overlay, tropical-water classification or a freshwater indication. Color uses regional LOD heights, not bathymetric surveys or the separate settlement detail mesh. Ground materials, coastal geology, seasonal appearance and broader landscape beauty remain ongoing work.
