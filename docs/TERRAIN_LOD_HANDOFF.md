# Landscape iteration 4 — regional landforms and continental coastlines

READY from `/Users/seanpurtill/Documents/Codex/tt-terrain-lod`, branch `codex/terrain-lod`, base `504be6a2b3c732761ae6ea21bb9278f03ffea485`. Sole integrator; owns the bounded rendering-density policy, its LocalTerrain streaming/visibility integration, unexplored terrain/water shading, and focused geometric/native audits. Shared `local_terrain.gd` has no concurrent edits or integration conflicts.

The old regional terrain stopped streaming above a 760 km camera height and disappeared at 820 km. Continent view therefore fell back to the global 481 × 241 mesh: approximately 83 km between samples. Regional patches also fell to 161 vertices per side. The visible angular shores and broad missing ridges were sampling artifacts, not the geography used by the simulation.

Terrain now continues refining through continental distance. Regional/continental detail is bounded at 513 × 513 vertices, using the same physical height, climate, woodland and geology functions. Close 385/257 targets remain for small patch spans. Geometric zoom buckets and snapped centers avoid per-frame rebuilds. The view footprint includes window aspect and tilt; global terrain remains outside the last completed patch during all transitions. New meshes install atomically. The water bed texture is rebound through the existing shared installation path.

Quick regional previews refine through 33, 129 and final density. Continental previews start sufficiently dense to avoid spacing worse than the global mesh. Latest camera requests cancel stale work. Only final meshes enter a cache capped at four entries AND 600,000 total vertices. This is a rendering budget, not a simulation or opponent restriction. Streamed terrain is clipped to the finite physical planet boundary rather than generating unplayable land beyond it.

Unknown terrain and water now share an unlit fog veil. Their normals can no longer reveal unseen mountains or shores. Native captures of hidden relief, plains and ocean produce byte-identical pixels. Revealed materials, waves and physical ocean level retain their behavior.

Validation: **54 source tests pass**, zero errors/failures/skips/orphans (`/tmp/tt-lod-source-tests.log`). Twelve suites cover LOD, physical fields, regional/close mesh builders, PlanetEnvironment, water, cover/resources/depletion, triangle sampling and distance controls. New checks cover rotated/tilted/wide footprint margins, cache/density budgets, progressive refinement, continental streaming, cached return and stale-job cancellation. In the fixed physical seed audit, regional height interpolation RMSE improves from **77.9 m to 11.8 m** over 1,333 points. Wrong land/water classification falls from **84 to 37** among 251 coastal samples. These are measured samples, not a claim of zero coastline error everywhere.

The native Compatibility audit passes all four live camera distances and inspects before/after regional and continental captures, close views, fog and the planet boundary. Ten captures are under ignored `artifacts/terrain-lod/`; no interactive game or player save is used. The probe exits normally (`/tmp/tt-lod-native-final.log`). At 1080 × 720 on M1 Pro, continental spacing is 14.61 km instead of ~83 km; regional spacing is 0.57 km. New regional coverage appears at 40 ms and the intermediate grid at 839 ms; continental preview appears at 658 ms. Full detail finishes in 7.4–14.6 seconds while rendering continues. Requested 5 ms construction slices observed maxima of 5.5–13 ms, and uploads took at most 14.3 ms. Isolated capture frame p95 is 17.5–28.1 ms; this is not a populated-game performance claim. A concurrent headless boot contributed load during this audit. Normal project boot/exits cleanly (`/tmp/tt-lod-main-boot.log`).

Save format, physical heights, climate, resources, travel, founding, water availability and human/opponent rules are unchanged. Rendering refreshes on restart. The old global mesh remains an immediate fallback; very wide/near-horizon views can still display it outside detailed coverage. Continental coastlines are more accurate but remain sampled, and full refinement takes seconds. These changes do not add new erosion, seasonal snow or vegetation species. Regional silhouettes still reflect the existing generator; close surface precision near the extreme world coordinates and further natural material/seasonal variety remain follow-up work. The landscape iteration is ongoing.
