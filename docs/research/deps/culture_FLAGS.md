# Culture flags

Registry years that look wrong given their prerequisites. The registry was not changed.

| Id (year) | Issue | Suggestion |
|---|---|---|
| `house_floor_burial` (9) | It comes before durable houses (`adobe_wall_construction` 18, `framed_construction` 20). It is mapped as an any-route, allowed only because the bands overlap. | Move it to about 18–20. |
| `ochre_grave_goods` (10) | It comes before `mineral_pigment_preparation` (18). It works with raw ochre, so the pigment link is only a precedent. | Minor. Consider about 15. |
| `plastered_ancestor_skulls` (16) | The historical plaster was lime, but `lime_burning` is at 50. It is mapped as clay modelling. | Keep 16 if clay is meant. Move to 50 or later if lime is meant. |
| `festival_calendar` (20) | It comes before `moon_counting` (22). It is mapped through ecology `phenology_signs` (18). | Minor. It is acceptable, or consider about 25. |
| `burial_ground_separation` (health 272) | Culture's alias "cemeteries" is at 140, and `graveside_libations` (135) implies graves set apart. | Pull the canonical year toward 140–170. |
| `dream_interpretation` (470) | Oral dream reading needs no writing or temple bureaucracy. | Move it to about 150 near `first_shrine_house`, or reword it as recorded dream-omens. |
