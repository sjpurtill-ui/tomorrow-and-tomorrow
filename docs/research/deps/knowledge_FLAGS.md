# Knowledge flags

Registry years that look wrong given their prerequisites. The registry was not changed.

| Id (year) | Issue | Suggestion |
|---|---|---|
| `balance_beam_weights` (320) | Production `standard_weight_sets` (165) comes 155 years before any balance. Stone weight sets are only useful with a balance. | Move `standard_weight_sets` to about 300, or split it into balance-free reference stones. |
| `sworn_interpreters` (445) | Oral interpreters came long before bilingual lists. It is mapped with an any-route through `trade_colonies`. | Consider about 300 if the entry means spoken interpreters. |
| `rod_and_cord_leveling` (240) | It comes after `river_supply_channels` (105) and `flood_levees` (135), which used eyeballed grades. | No change. The early channels are read as unlevelled work. |

## Overlap resolutions (mapped as links, not flags)

- `watch_duty_rotation` requires security `watch_rotation`.
- `messenger_relay_stations` requires labor `runner_relays_between_work_sites`.
- `balance_beam_weights` requires production `standard_weight_sets`.
