# Labor year flags

Registry years left unchanged. Items whose year looks off given their mapped prerequisites:

- `children_light_tasks` (16): merged alias at demography 32. Keep 16; it needs only `household_task_division` (2).
- `measured_plot_allotment` (425): falls before `geometric_survey` (450) and `area_volume_rules` (470). It is mapped on `standard_measures` + `field_boundary_markers`, with the survey rules as precedents. If allotment must use area rules, move it to about 470.
- `brick_quotas` (430) and `standard_brick_proportions` (430) share a year. Each is only a precedent of the other, so there is no cycle.
- `gangs_of_ten` (355): depends on `work_gang_overseers` (institutions 180). The 175-year gap is plausible, because decimal gangs came with the corvée.
- `crew_loans_between_cities` (600): needs a treaty or pass id. `foreign_treaties` (440), `boundary_treaties` (410) and `sealed_travel_passes` (445) are all earlier, so the year is fine.
